apiVersion: apps/v1
kind: Deployment
metadata:
  name: udagram-api-feed-app
  labels:
    app: udagram-api-feed-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: udagram-api-feed-app
  template:
    metadata:
      labels:
        app: udagram-api-feed-app
    spec:
      containers:
        - name: udagram-api-feed
          image: lajcik007/udagram-api-feed
          ports:
            - containerPort: 8080
          resources:
            requests:
              memory: "64Mi"
              cpu: "250m"
            limits:
              memory: "1024Mi"
              cpu: "500m"